# Homebrew Tap

Personal Homebrew tap for custom formulae and casks.

## Installation

```sh
brew tap gaoyifan/tap
brew install --cask openwarp
```

## Update

```sh
brew update
brew upgrade --cask openwarp
```
